package Enhancedfor;

public class ifff{ //Public class main
    public static void main(String[] args){ //untuk menjalankan program
        int i = 6; //Menentukan Nilai i
        System.out.println((i > 3) ? "i is greater than 3" : (i < 3) ? "i is smaller than 3" : "i is equal to 3"); 
        //Membuat if else menggunakan ? agar bisa di kerjakana hanya dengan 1 line saja. 
        //? berfungsi sebagai penanda boolean yang bisa di fungsikan juga sebgai if/else if. 
        //Yang dimana jika program yang berada di kiri true maka program yang di kiri akan di jalankan. 
        //Sedangkan jika false dia akan melanjutkan kodingan setelah :
    }
}
